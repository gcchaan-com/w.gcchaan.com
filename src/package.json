{
  "private": true,
  "name": "@gcchaan/w.gcchaan.com",
  "scripts": {
    "start": "hugo server -D",
    "prebuild": "git submodule update --init --recursive",
    "build": "hugo"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "postcss-cli": "^9.1.0"
  }
}
