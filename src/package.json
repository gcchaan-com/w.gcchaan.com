{
  "private": true,
  "name": "@gcchaan/w.gcchaan.com",
  "scripts": {
    "start": "hugo server -D",
    "prebuild": "git submodule update --init --recursive",
    "build": "hugo"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss-cli": "^8.3.1"
  }
}
